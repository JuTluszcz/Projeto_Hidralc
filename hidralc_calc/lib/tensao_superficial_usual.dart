import 'package:flutter/material.dart';

class TensaoSuperficialUsual extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("TensaoSuperficialUsual"),
      ),
    );
  }
}