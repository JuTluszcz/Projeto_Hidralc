import 'package:flutter/material.dart';

class Potencia extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Potencia"),
      ),
    );
  }
}