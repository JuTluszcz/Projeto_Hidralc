import 'package:flutter/material.dart';

class Velocidade extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Velocidade"),
      ),
    );
  }
}