import 'package:flutter/material.dart';

class DensidadesRelativasUsuais extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Densidades Relativas Usuais"),
      ),
    );
  }
}